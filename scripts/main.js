// ============================================
// 牡丹亭 Mod — 主入口
// ============================================

log("牡丹亭 Mod 已加载 ✓");

// 加载物品/液体注册文件。
// 顺序很关键：游戏先执行 main.js（JS 建对象登记），再解析 content/ 的 JSON（填属性）。
// 所以 require 必须在最前面（详见 debug-004 第六节）。
require("items");
// 方块注册在物品之后（方块配方会引用物品，物品对象必须先存在）。
require("blocks");
